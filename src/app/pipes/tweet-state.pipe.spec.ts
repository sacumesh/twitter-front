import { TweetStatePipe } from './tweet-state.pipe';

describe('TweetStatePipe', () => {
  it('create an instance', () => {
    const pipe = new TweetStatePipe();
    expect(pipe).toBeTruthy();
  });
});
